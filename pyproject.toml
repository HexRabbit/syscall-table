[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = "^3.11"
halo = "^0.0.31"
lxml = "^5.2.1"
python-ctags3 = "^1.6.0"
requests = "^2.31.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
